spring.application.name=autoflex

# H2 (development) - use for quick local run without PostgreSQL
# spring.datasource.url=jdbc:h2:mem:autoflexdb;DB_CLOSE_DELAY=-1
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.h2.console.enabled=true
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=update

# Uncomment below and comment H2 above to use PostgreSQL
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# Dynamic Port Configuration for Render/Heroku Support
server.port=${PORT:8080}
